# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
build/
*.local

# Vercel
.vercel

# Environment variables (secrets — NEVER commit)
.env
.env.local
.env*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS files
.DS_Store
Thumbs.db
nul

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# Claude Code : constitution & etat de l'agent local (jamais sur le repo public)
CLAUDE.md
.claude/

# Docs internes (audit VPS, architecture) : sensibles, ne pas exposer (repo public)
docs/

# Legacy / one-shot SQL scripts (already applied to Supabase)
database-migrations.sql

# Backup files (deleted before deploy per project convention)
*.backup-*
*.previous-version

# Client data exports (PII — never commit, repo is public)
src/assets/clients-export-*.csv
src/assets/prospects-*.csv
data/clients-export-*.csv
data/prospects-*.csv
