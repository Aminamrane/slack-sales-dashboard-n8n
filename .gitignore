# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
build/
*.local

# Vercel
.vercel

# Environment variables (secrets — NEVER commit)
.env
.env.local
.env*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS files
.DS_Store
Thumbs.db
nul

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# Claude Code : constitution & etat de l'agent local (jamais sur le repo public)
CLAUDE.md
.claude/

# Docs internes (audit VPS, architecture) : sensibles, ne pas exposer (repo public)
docs/

# Legacy / one-shot SQL scripts (already applied to Supabase)
database-migrations.sql

# Backup files (deleted before deploy per project convention)
*.backup-*
*.previous-version

# Client data exports (PII — never commit, repo is public)
src/assets/clients-export-*.csv
src/assets/prospects-*.csv
data/clients-export-*.csv
data/prospects-*.csv

# Registre local des depenses du Bot IA (contient des emails — jamais commite)
.data/

# Infra/accès SSH prod (hosts, users, commandes, clés) — jamais commité, repo public
connexions-ssh-owner.txt
api/connexions-ssh-owner.txt
ovh_migration
api/ovh_migration
*.pem
*.key
*.p12
*.pfx
id_ed25519*
id_rsa*

# Variantes de .env (secrets) — seul .env.example se commite
.env.*
!.env.example

# Sauvegardes d'infra (.env.bak-…, page.tsx.bak-…) : peuvent contenir des secrets
*.bak
*.bak-*
*.bak.*

# Constitution de l'agent Sub Tickets (procédure d'accès au VPS de prod) :
# repo public -> jamais commitée, comme CLAUDE.md
/instruction_file_dattente.md
/mission.md
